# Dependencies
node_modules/
.pnpm-store/

# Build outputs
.next/
out/
dist/
build/
*.tsbuildinfo

# Turborepo
.turbo/

# Env (#40): ignore every .env* file except the committed example/template
.env*
!.env.example

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# OS
.DS_Store

# Editors
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/

# Testing
coverage/

# Eval suite (#72) report artifact — generated per-run, never committed.
packages/agent/eval-report.json

# Playwright
/test-results/
/playwright-report/
/playwright/.cache/
/blob-report/
apps/web/e2e/screenshots/

# Preview e2e gate (#58)
/test-results-preview/
/playwright-preview-report/

# Lighthouse gate (#58) — .lighthouserc.local.json is generated per-run and
# may embed the Vercel Deployment Protection bypass header; never committed.
/.lighthouseci/
/.lighthouserc.local.json

# Downloadable CV PDF (#35) — regenerated from packages/career-data by
# `pnpm generate:cv`, wired into the web build; never committed.
apps/web/public/cv/

# Claude Code agent worktrees (created by isolated agent sessions; never tracked)
.claude/worktrees/
